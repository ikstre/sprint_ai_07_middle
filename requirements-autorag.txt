# ============================================================
# 유저 실행환경(user env) 의존성 (Scenario A AutoRAG 전용)
#
# 역할:
#   - A안 AutoRAG 최적화 / dashboard / api / web
#   - shared env와 분리된 사용자별 실험 환경
#
# 설치 예:
#   conda create -n autorag python=3.11
#   conda activate autorag
#   pip install -r requirements-autorag.txt
#
# 연동:
#   export AUTORAG_PYTHON=/path/to/envs/autorag/bin/python
#   python scripts/run_autorag_optimization.py ...
# ============================================================

# user env 전용 AutoRAG 런타임
AutoRAG==0.3.22
langchain-community>=0.4.0
langchain-text-splitters>=1.1.1
openai>=2.30.0

# 기본 유틸리티
pandas>=2.2.0
numpy>=2.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0
pyarrow>=16.0.0
