# Secrets / local env
.env
.env.local
.env.*.local
myenv/

# Raw input data (do not commit)
data/
!data/.gitkeep

# Vector DB / indexes
chroma_db/
data/vectordb/

# Evaluation outputs (project-root only)
/evaluation/

# Finetuned model weights (large binary files — use NAS or shared storage)
models/finetuned/

# Local shared data (models, datasets — use NAS or shared storage)
shared_data/

# CUDA installer
cuda-keyring*.deb

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
evaluation/eval_results.json

# Logs
*.log
